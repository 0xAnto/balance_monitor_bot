version: '3.8'

services:
  balance-monitor-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: balance-monitor-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - NODE_ENV=production
